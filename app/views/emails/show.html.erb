<%= turbo_frame_tag "voir_message" do %>
  <h1><%= @email.object %></h1></br>
  <p><%= @email.body %></p>
  <%= button_to "Supprimer", email_path(@email.id), method: :delete, data: { turbo_frame: "nouvel_email" } %>
  <br>
  <%= button_to "Marquer comme non lu", @email, method: :patch,  data: { turbo_frame: "nouvel_email" }  %>


<% end %>
